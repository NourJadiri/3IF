Ce test permet de vérifier si la sortie est correcte
lorsqu'il n'y a que des erreurs dans le fichier de log,
dans le cadre de l'usage par défaut de l'application. Il
vérifie donc qu'il n'y a aucun hit d'affiché.
